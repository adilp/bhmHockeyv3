# BHM Hockey - Environment Variables Template
# Copy this file to .env for local development
# For production, set these in Digital Ocean App Platform console

# ===================================
# Database Configuration
# ===================================
# Local Development: Use your local PostgreSQL connection
# Production: Digital Ocean App Platform auto-injects DATABASE_URL
ConnectionStrings__DefaultConnection=Host=localhost;Port=5433;Database=bhmhockey;Username=bhmhockey;Password=yourpassword

# ===================================
# JWT Authentication
# ===================================
# Note: In development mode, API auto-generates a default secret if not provided
# For production: Generate with: openssl rand -base64 32
Jwt__Secret=dev-secret-key-for-local-development-only-min-32-chars
Jwt__Issuer=http://localhost:5001
Jwt__Audience=http://localhost:5001
Jwt__ExpiryMinutes=60
Jwt__RefreshExpiryDays=7

# ===================================
# CORS Configuration
# ===================================
# Note: In development, CORS is automatically set to allow all origins
# For production, add your actual mobile app URLs:
# Cors__AllowedOrigins=https://yourdomain.com,exp://your-app

# ===================================
# Expo Push Notifications
# ===================================
# Get this from https://expo.dev after creating your project
# Go to: Project Settings → Access Tokens
# Expo__AccessToken=

# ===================================
# ASP.NET Core Configuration
# ===================================
ASPNETCORE_ENVIRONMENT=Development

# Note: API automatically binds to http://0.0.0.0:5001 in development
# This allows React Native apps to connect from emulators/devices
# For production, App Platform uses: http://+:8080

# ===================================
# Logging
# ===================================
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning

# ===================================
# Production Notes
# ===================================
# These environment variables are set in Digital Ocean console:
# - Jwt__Secret (generate with: openssl rand -base64 32)
# - Expo__AccessToken (from expo.dev)
# - ConnectionStrings__DefaultConnection (auto-injected)
# - Cors__AllowedOrigins (your actual app URL)
# - ASPNETCORE_URLS (set to http://+:8080)
